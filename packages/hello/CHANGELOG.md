# CHANGELOG — hello

This is the changelog for the `hello` pis package. It is independent of the
pis package manager's own changelog.

## 0.1.0 — 2026-09-12
- Initial release.
- `greet(name="pis")` — returns a friendly greeting string.
- `main()` — prints the greeting, returns 0.
- Script entry point: `greet = "hello:main"`.
